## 2026-01-22 - Keyboard Types for Transport Lines
**Learning:** Transport line identifiers often contain letters (e.g., "550B", "M1"), so restricting input to `.numberPad` blocks valid searches.
**Action:** Always use `.default` or `.namePhonePad` for line search inputs unless strictly numeric IDs are guaranteed by the backend.
